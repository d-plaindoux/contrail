import org.wolfgang.contrail.connection.net.Net
import org.wolfgang.contrail.network.ecosystem.Echo

def typeCase = fun type -> coerce;

def MainFlow = fun b -> 
	b <> payload <> object <> typeCase "java.lang.String" <> echo;

def Server = fun uri flow -> server;

Server "tcp://localhost:6666" MainFlow;
